- if @resources.blank?
  .blank_notice
    暂时没有下载资源...
- else      
  - @resources.each do |res| 
    .res_summary
      %span.season= res.season.to_s << res.episode.to_s 
      %span.size= res.episode_size
      %span.ext= res.episode_format
      %a.download_link{ :href => 'javascript:;' }= res.link_text
.pagination_panel
  = will_paginate(@resources, :class => 'digg_pagination')