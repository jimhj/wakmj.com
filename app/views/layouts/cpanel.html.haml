<!DOCTYPE html>
%html
  %head
    %title 后台管理
    = stylesheet_link_tag 'app', 'cpanel', 'lib/bootstrap.min'
    = yield :header_css
    = javascript_include_tag 'app', 'lib/bootstrap.min'
    = yield :header_js
    = csrf_meta_tags
    %meta{ :charset => 'utf-8' }
    = ico_tag
  %body
    = render 'layouts/shared/admin_header'
    .main_container.clearfix
      .cpanel
        - if flash[:error] || flash[:success]
          = render "cpanel/shared/flash_notice"         
        = yield
    = yield :footer_script 